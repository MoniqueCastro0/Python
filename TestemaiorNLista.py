#Maior n de uma lista;

#lista = [3, 7, 8, 6, 10, 2] #lista = []  - lista.append(3)
#print(max(lista))

#for i in lista: #percorre cada posição e mostra os números
    #print(i)

#quantidadeNumDigitados = int(input("Qunatos números você vai digitar? "))

#for a in range.(quantidadeNumDigitados): #enquanta a não tiver o valor de quantiadeNumDigitados faça
    #num = float(input(f"Digite o {a + 1}")) #a começa em 1, cada vez que o loop for repetir vai contar mais 1
    #nums.append(num) #adiciona em uma lista os valores de num